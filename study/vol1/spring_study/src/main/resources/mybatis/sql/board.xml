<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.spring_study.mvc.repository.BoardRepository">
    <select id="getList" resultType="com.example.spring_study.mvc.domain.vo.Board">
        SELECT
            B.BOARD_SEQ,
            B.BOARD_TYPE,
            B.TITLE,
            B.CONTENTS,
            B.REG_DATE
        FROM T_BOARD B
        ORDER BY B.REG_DATE DESC
    </select>

    <select id="get" parameterType="int" resultType="com.example.spring_study.mvc.domain.vo.Board">
        SELECT
            B.BOARD_SEQ,
            B.BOARD_TYPE,
            B.TITLE,
            B.CONTENTS,
            B.REG_DATE
        FROM T_BOARD B
        WHERE B.BOARD_SEQ = #{boardSeq}
    </select>

    <insert id="save" parameterType="com.example.spring_study.mvc.domain.dto.BoardRequest" useGeneratedKeys="true" keyProperty="boardSeq">
        INSERT INTO T_BOARD (
            BOARD_TYPE,
            TITLE,
            CONTENTS,
            REG_DATE
        ) VALUES (
            #{boardType.code},
            #{title},
            #{contents},
            NOW()
        )
    </insert>
    <insert id="saveList" parameterType="map">
        INSERT INTO T_BOARD (
            BOARD_TYPE,
            TITLE,
            CONTENTS,
            REG_DATE
        ) VALUES
        <foreach collection="boardList" item="board" separator=",">
        (
            #{board.boardType.code},
            #{board.title},
            #{board.contents},
            NOW()
        )
        </foreach>
    </insert>

    <update id="update" parameterType="com.example.spring_study.mvc.domain.dto.BoardRequest">
        UPDATE T_BOARD
        SET
            BOARD_TYPE = #{boardType.code},
            TITLE = #{title},
            CONTENTS = #{contents}
        WHERE BOARD_SEQ = #{boardSeq}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM T_BOARD
        WHERE BOARD_SEQ = #{boardSeq}
    </delete>
</mapper>